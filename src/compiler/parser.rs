//! Parsing boundary.

/// The parser stage is reserved for a future milestone.
#[derive(Debug, Default)]
pub struct Parser;

impl Parser {
    /// Construct the parser placeholder.
    pub const fn new() -> Self {
        Self
    }
}

