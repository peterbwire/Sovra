//! Lexical analysis boundary.

/// The lexer stage is reserved for M1.
#[derive(Debug, Default)]
pub struct Lexer;

impl Lexer {
    /// Construct the M0 lexer placeholder.
    pub const fn new() -> Self {
        Self
    }
}

